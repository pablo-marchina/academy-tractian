# E9 v4.1 groundedness-surface structural CI result

**Date:** 2026-08-18  
**Scope:** oracle-free structural validity only

The preregistered one-sided public groundedness-surface diagnostic passed its synthetic CI contract in workflow `research-e9-v4`, run `32206819810`.

Validated checks include:

- visible concrete identifier provenance;
- placeholder identifier exclusion;
- public METHOD+path normalization through the v4.1 normalizer;
- unrecognized endpoint detection;
- visible unit-bearing numeric provenance;
- unsupported unit-bearing numeric detection;
- false `trace_quality_self_check` flag detection;
- aggregate-only/privacy-safe reporting;
- explicit one-sided interpretation: zero findings does **not** establish general semantic groundedness.

No private oracle, private scorer rows, VALIDATION feedback or LOCKED_TEST material was used by the synthetic check.

This diagnostic may now be run on fixed DEV captures as an evaluator-validity diagnostic. It cannot authorize VALIDATION and cannot change the candidate.
